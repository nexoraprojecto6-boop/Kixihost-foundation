# Ambientes

`development`, `staging`, `production` — cada um com configurações,
secrets, base de dados, Redis e (quando necessário) infraestrutura
próprios. Nunca misturar produção com desenvolvimento.
