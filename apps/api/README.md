# @kixihost/api

Control Plane HTTP API (NestJS). Responsável por autenticação,
utilizadores, GitHub, projectos, deployments (orquestração), billing,
pagamentos, domínios, logs, monitorização, notificações, administração
e segurança — nunca pela execução do código dos clientes.
